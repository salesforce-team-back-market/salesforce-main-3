<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Used to exclude API users from the password policy to reset password every 90 days for all users. This way integrations won&apos;t be blocked every 90 days until the password (and code) is changed.</description>
    <hasActivationRequired>false</hasActivationRequired>
    <label>Password Never Expires</label>
    <license>Salesforce</license>
    <userPermissions>
        <enabled>true</enabled>
        <name>PasswordNeverExpires</name>
    </userPermissions>
</PermissionSet>
